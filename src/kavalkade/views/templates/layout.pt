<!doctype html>
<html>
    <head>
        <title>Kavalade</title>
        <link rel="stylesheet" href="style.css" />
        <style>
        :root {
    --dark-blue: #22223bff;
    --grayish-blue: #4a4e69ff;
    --gray: #9a8c98ff;
    --faded-brown: #c9ada7ff;
    --sand: #f2e9e4ff;
}
html, body{
    margin:0;
    padding:0;
}

body{
    background:var(--dark-blue);
    display:grid;
    grid-gap:0px;
    grid-template-columns:300px auto;
    grid-template-rows:100vh;
    grid-template-areas: "sidemenu" "main";
}

nav{
    background:var(--grayish-blue);
    grid-area: sidemenu;
    box-shadow:3px 3px 3px
}

section{
    background:
    grid-area:main;
    position:relative;
}
</style>
    </head>
    <body>
        <nav>

            <div tal:replace="structure slot: menu" ></div>

        </nav>
        <section class="col-md-12" tal:content="structure content"></section>
        <script type="text/javascript" src="app.js"></script>
    </body>
</html>
